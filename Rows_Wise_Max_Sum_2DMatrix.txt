
#include <stdio.h>
#include<math.h>
int main() 
{
    int r,c;
    printf("Enter the r and c:");
    scanf("%d %d", &r,&c);
    int arr[r][c];
    printf("Enter elements:");
    for(int i=0;i<r;i++)
    {
        for(int j=0;j<c;j++)
        {
            scanf("%d",&arr[i][j]);
        }
    }
    int arr2[r];
    for(int i=0;i<r;i++)
    {
        int row_sum =0;
        for(int j=0;j<c;j++)
        {
            row_sum += arr[i][j];
            arr2[i] = row_sum;
        }   
    }
    int cur_max = arr2[0];
    for(int i=0;i<r;i++)
    {
       cur_max = fmax(cur_max,arr2[i]); 
    }
    printf("%d",cur_max);
}
     
    