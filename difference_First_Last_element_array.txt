// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n;
    printf("Enter n:");
    scanf("%d",&n);
    int arr[n];
    printf("Enter array elements ony 1,2,3\n:");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int diff = arr[0] - arr[n-1];
    
    printf("difference is:%d",diff);
    return 0;
}