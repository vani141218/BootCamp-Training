// Maximum Sub-array in the array.
#include<math.h>
#include <stdio.h>
int main() 
{
    int n;
    printf("Enter n:");
    scanf("%d",&n);
    int arr[n];
    int max_sum =0;
    printf("Enter elements:");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    
    for(int i=0;i<n;i++)
    {
        int curr_sum =0;
        for(int j=i;j<n;j++)
        {
           curr_sum += arr[j];
           max_sum = fmax(curr_sum,max_sum);
        }
    }
    printf("%d",max_sum);
}


     
    